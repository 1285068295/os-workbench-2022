{
    // ctrl+shift+p打开命令行，输入Tasks: Run task==》 Create tasks.json file from template， 
    // 生成默认的tasks.json文件。
    // tasks.json是用来设置指令编译代码，launch.json是设置执行环境来执行代码。setting.json是设置语言环境
    // 将 .vscode_M2  文件夹名称改为 .vscode

    "files.associations": {
     
    }
}